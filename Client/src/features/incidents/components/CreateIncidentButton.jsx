import Button from '../../shared/components/Button'

function CreateIncidentButton({ disabled = false }) {
  return <Button disabled={disabled}>Create Report</Button>
}

export default CreateIncidentButton
